## MediaBlade defaults
# Bind published ports to localhost by default (safer).
# To expose to LAN, set BIND_IP=0.0.0.0 (or your LAN IP).
BIND_IP=127.0.0.1

# Timezone for containers
TZ=Etc/UTC

# Run containers as your user (recommended).
# Set to `id -u` / `id -g` of the user who owns ./media and ./downloads.
MEDIABLADE_UID=1000
MEDIABLADE_GID=1000

# Log rotation for docker json-file logging driver
LOG_MAX_SIZE=10m
LOG_MAX_FILE=3

## Reverse proxy (Traefik) - optional
# Start Traefik with: docker compose --profile proxy up -d
TRAEFIK_ENABLE=false
TRAEFIK_BIND_IP=0.0.0.0
TRAEFIK_ENTRYPOINTS=websecure
TRAEFIK_TLS=true
TRAEFIK_CERTRESOLVER=le
TRAEFIK_LOG_LEVEL=INFO
TRAEFIK_DASHBOARD=false

# Required if you enable Traefik + Let's Encrypt
LETSENCRYPT_EMAIL=you@example.com

# Hostnames for services (used by Traefik labels)
JELLYFIN_HOST=jellyfin.example.com
MEDIAMANAGER_HOST=mediamanager.example.com
WIZARR_HOST=wizarr.example.com

## Tdarr GPU options
# NVIDIA: install NVIDIA Container Toolkit on the host.
# Use `all` or a number like `1`.
TDARR_NVIDIA_GPUS=0
NVIDIA_VISIBLE_DEVICES=
NVIDIA_DRIVER_CAPABILITIES=compute,video,utility

# Intel/AMD: if you uncomment /dev/dri mapping in compose, set the render group id:
RENDER_GID=0

